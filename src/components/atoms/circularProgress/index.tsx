import { CircularProgress } from '@mui/material'
import React, { FC } from 'react'

const MuiCircularProgress: FC = () => {
  return <CircularProgress sx={{ mt: '10rem' }} />
}

export { MuiCircularProgress }
